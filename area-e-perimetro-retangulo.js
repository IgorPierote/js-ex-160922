const baseRet = Number (prompt("Insira a base do retangulo"))
const alturaRet = Number (prompt("Insira a altura do retangulo"))

let resArea = baseRet * alturaRet

let resPeri = (2 * (baseRet + alturaRet))

alert("A área do retangulo é de: " + resArea)
alert("O perimetro do rentagulo é de: " + resPeri)